package com.folio.app.core.designsystem.theme

import androidx.compose.ui.graphics.Color

/**
 * Folio's palette is deliberately tiny. The whole app is built from "paper" and
 * "ink" plus one restrained accent. No gradients, no saturated chrome — the
 * content is the colour.
 */

// Light — warm off-white paper, soft ink.
val PaperLight = Color(0xFFFAF8F4)
val PaperRaisedLight = Color(0xFFFFFFFF)
val InkLight = Color(0xFF1C1B19)
val InkMutedLight = Color(0xFF6F6B63)
val HairlineLight = Color(0xFFE7E3DB)
val AccentLight = Color(0xFF8A6A4B) // muted leather/binding brown

// Dark — true black for AMOLED, warm bone text.
val PaperDark = Color(0xFF0B0B0C)
val PaperRaisedDark = Color(0xFF161617)
val InkDark = Color(0xFFEDE9E1)
val InkMutedDark = Color(0xFF8E8A82)
val HairlineDark = Color(0xFF242425)
val AccentDark = Color(0xFFC9A07A)
